# Lines starting with '#' and sections without content
# are not displayed by a call to 'details'
#
[paths]
# Paths related to this bug.
# suggested format: REPO_PATH:LINENUMBERS


[details]
# Additional details


[expected]
# The expected result


[actual]
# What happened instead


[reproduce]
# Reproduction steps


[comments]
# Comments and updates - leave your name


On: Tuesday, December 04 2012 07:48AM
This should be a list of configuration properties, with potential descriptions and default values, that can be set based on a single configuration class. The output should be done in such a way that a diff with an old report should be informative. ie. if a property was removed, the diff shows that clearly, if one was added, it also shows that clearly.